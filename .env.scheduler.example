# Scheduler Environment Configuration
# Copy this file to .env.scheduler and fill in your Azure details

# Server Configuration
PORT=8080
NODE_ENV=development

# Platform: podman (local) | azure (Azure Container Jobs)
PLATFORM=azure

# Scheduler Settings
SCHEDULER_MAX_CONCURRENT_JOBS=100

# Azure Configuration (required when PLATFORM=azure)
AZURE_SUBSCRIPTION_ID=your-subscription-id-here
AZURE_RESOURCE_GROUP=your-resource-group
AZURE_JOB_NAME=meet-teams-bot-job
AZURE_LOCATION=eastus

# Container Image (the bot image that will be triggered)
# This should point to your Azure Container Registry
SCHEDULER_CONTAINER_IMAGE=yourregistry.azurecr.io/meet-teams-bot:latest

# Azure Authentication
# The scheduler uses DefaultAzureCredential which supports:
# 1. Environment variables (AZURE_CLIENT_ID, AZURE_TENANT_ID, AZURE_CLIENT_SECRET)
# 2. Managed Identity (when running in Azure)
# 3. Azure CLI (az login) - easiest for local testing
#
# For local testing, just run: az login
# The DefaultAzureCredential will automatically use your CLI credentials
